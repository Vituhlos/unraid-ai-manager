[project]
name = "unraid-ai-manager"
version = "0.1.7"
description = "Safe Unraid automation helper and MCP integration for AI-assisted DockerMan template management."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
unraid-ai-manager = "unraid_ai_manager.cli:main"
